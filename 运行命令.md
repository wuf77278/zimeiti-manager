# 自媒体管家运行命令

项目目录：

```bash
cd /Users/Apple_501/Desktop/自媒体管家
```

## 首次准备

```bash
cp .env.example backend/.env   # 如需启用 LLM，请填写其中的 API Key
chmod +x start.sh              # 仅需一次
make install                   # 安装后端 venv 与前端依赖
make data                      # 初始化 SQLite baseline 数据
```

## 一键启动

```bash
./start.sh
```

启动后访问：

- 前端：http://localhost:5173/app
- 后端：http://localhost:8001
- API 文档：http://localhost:8001/docs

## 手动启动

后端：

```bash
cd /Users/Apple_501/Desktop/自媒体管家/backend
./venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload
```

前端：

```bash
cd /Users/Apple_501/Desktop/自媒体管家/frontend
npm run dev -- --host 127.0.0.1 --port 5173
```

## 测试与检查

后端测试：

```bash
cd /Users/Apple_501/Desktop/自媒体管家
make test
# 或
cd backend && ./venv/bin/python -m pytest tests/ -v
```

前端类型检查与构建：

```bash
cd /Users/Apple_501/Desktop/自媒体管家/frontend
npx tsc --noEmit
npx vite build
```

说明：前端使用 `/app` 作为路由 basename，因此本地访问入口应为 `http://localhost:5173/app`。
